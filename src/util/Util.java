/*@author Samir Hasanov */
package util;

public class Util {
//       -*- without order by sort by map -*-
//        Map<String, Integer> map = new TreeMap<>();
//        IncomeRepository incomeRepository = new IncomeRepository();
//        IncomeCategoryRepository categoryRepository = new IncomeCategoryRepository();
//        List<Income> incomeList = incomeRepository.findAll();
//        List<IncomeCategory> categoryList = categoryRepository.findAll();
//        for (IncomeCategory c : categoryList) {
//            for (Income i : incomeList) {
//                if (c.getName().equals(i.getIncomeCategory().getName())) {
//                    if (map.containsKey(c.getName())) {
//                        map.put(c.getName(), i.getAmount() + map.get(c.getName()));
//                    } else {
//                        map.put(c.getName(), i.getAmount());
//                    }
//                }
//            }
//        }
//        ObservableList<PieChart.Data> pieChartData = FXCollections.observableArrayList();
//        for (String c : map.keySet()) {
//            if (map.get(c) > 0) {
//                pieChartData.add(new PieChart.Data(c, map.get(c)));
//            }
//        }
//        chart.setData(pieChartData);
}
